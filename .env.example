# Copy to `.env.local` and fill in your own OAuth client ID.
# Without this, the dashboard runs in "local-only" mode (data persists to
# browser localStorage but does not sync to Google Drive).
#
# Setup steps in README.md → "Drive sync — OAuth client setup".

VITE_GOOGLE_CLIENT_ID=
